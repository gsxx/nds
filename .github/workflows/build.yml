name: Build NDS Homebrew

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest  # Also works on Fedora if needed
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install devkitPro
        run: |
          sudo apt-get update -y
          sudo apt-get install -y wget
          wget https://github.com/devkitPro/pacman/releases/download/devkitpro-pacman-3.0.2/devkitpro-pacman.deb
          sudo dpkg -i devkitpro-pacman.deb
          sudo dkp-pacman -Syu --noconfirm
          sudo dkp-pacman -S --noconfirm devkitARM libnds nds-tools

      - name: Set up environment
        run: |
          echo "DEVKITPRO=/opt/devkitpro" >> $GITHUB_ENV
          echo "DEVKITARM=/opt/devkitpro/devkitARM" >> $GITHUB_ENV
          echo "$DEVKITARM/bin:$PATH" >> $GITHUB_PATH

      - name: Compile NDS Homebrew
        run: make

      - name: Upload NDS ROM
        uses: actions/upload-artifact@v4
        with:
          name: game.nds
          path: ./your_game.nds  # Change to match your output filename
